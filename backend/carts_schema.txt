                                  Table "public.carts"
    Column    |            Type             | Collation | Nullable |      Default       
--------------+-----------------------------+-----------+----------+--------------------
 id           | uuid                        |           | not null | uuid_generate_v4()
 user_id      | uuid                        |           |          | 
 session_id   | character varying(255)      |           |          | 
 promotion_id | uuid                        |           |          | 
 expires_at   | timestamp without time zone |           |          | 
 created_at   | timestamp without time zone |           |          | CURRENT_TIMESTAMP
 updated_at   | timestamp without time zone |           |          | CURRENT_TIMESTAMP
Indexes:
    "carts_pkey" PRIMARY KEY, btree (id)
    "carts_session_id_key" UNIQUE CONSTRAINT, btree (session_id)
    "carts_user_id_key" UNIQUE CONSTRAINT, btree (user_id)
    "idx_carts_expires_at" btree (expires_at)
    "idx_carts_session_id" btree (session_id)
    "idx_carts_user_id" btree (user_id)
Foreign-key constraints:
    "carts_promotion_id_fkey" FOREIGN KEY (promotion_id) REFERENCES promotions(id) ON DELETE SET NULL
    "carts_user_id_fkey" FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
Referenced by:
    TABLE "cart_items" CONSTRAINT "cart_items_cart_id_fkey" FOREIGN KEY (cart_id) REFERENCES carts(id) ON DELETE CASCADE
Triggers:
    update_carts_updated_at BEFORE UPDATE ON carts FOR EACH ROW EXECUTE FUNCTION update_updated_at_column()

